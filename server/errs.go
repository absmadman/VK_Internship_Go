package server

const (
	bindingError = `error binding json`
	costError    = `error cost cannot be negative`
	balanceError = `error balance cannot be negative`
	nameError    = `error name cannot be empty`
	dbError      = `error database indexing`
	paramError   = `error parameter type`
	userIdError  = `error invalid user_id`
)
